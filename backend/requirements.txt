fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
# httpx is available as a zero-dependency async option but the LLM client
# uses stdlib urllib to avoid extra deps; add if you need async LLM calls:
# httpx>=0.27.0
